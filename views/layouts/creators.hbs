{{> startfile}}
    {{> header}}

    <div class="row">
        {{#creators.creators}}
            <div class="col-sm-6 col-md-4">
                <div class="thumbnail">
                    <img src="{{img_src}}" alt="">
                    <div class="caption">
                        <h3>{{name}}</h3>
                        <p>{{description}}</p>
                        <p>
                            <a href="creators/{{username}}" class="btn btn-primary" role="button">Visit page</a>
                            <a href="#" class="btn btn-default" role="button">Add to following</a>
                        </p>
                    </div>
                </div>
            </div>
        {{/creators.creators}}
    </div>
    
    {{> footer}}
{{> endfile}}