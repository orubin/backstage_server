{{> startfile}}
    {{> header}}

    <br>
    <br>
    <br>
    <br>
    <br>

    <div class="row">
        {{#categories.categories}}
            <div class="col s12 m6">
                <div class="thumbnail">
                    <img src="{{img_src}}" alt="">
                    <div class="caption">
                        <h3>{{name}}</h3>
                        <p>{{description}}</p>
                        <p>
                            <a href="categories/{{id}}" class="btn btn-primary" role="button">Go there!</a>
                        </p>
                    </div>
                </div>
            </div>
        {{/categories.categories}}
    </div>

    {{> footer}}
{{> endfile}}